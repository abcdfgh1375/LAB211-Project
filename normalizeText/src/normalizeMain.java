import Controller.normalizeController;
public class normalizeMain {
    public static void main(String[] args) throws Exception {
        normalizeController no = new normalizeController();
        no.run();
    }
}
